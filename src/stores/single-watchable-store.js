// used by SingleWatchablePage to retrieve and store watchable details, video source, related watchables, belongsTo(if series)
// use api.get (from boot/axios.js) for api calls to avoid redundant code
